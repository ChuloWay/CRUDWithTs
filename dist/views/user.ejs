<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>
<body>
    <h1>A List Of Active Users</h1>
    <ul>
        <% for(let u of users) { %> 
        <li><%=u.user  %></li>
        <% } %> 
        </ul>
        <h1>Tasks</h1>
        <ul>
            <% for(let u of users) { %> 
            <li><%=u.user%>

            <ul>
                <% for(let x of u.todos) {%> 
                    <li><%=x.todo %> </li>
                <% } %> </ul>
                </li>
            <% } %> 
            </ul>
    
    
</body>
</html>